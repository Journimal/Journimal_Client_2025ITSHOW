import 'package:flutter/material.dart';

class InfromationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('인포메이션 화면입니다'),
    );
  }
}
