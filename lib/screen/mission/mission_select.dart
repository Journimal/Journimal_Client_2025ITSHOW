import 'package:flutter/material.dart';

class MissionSelectScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('미션고르는 화면입니다'),
    );
  }
}
